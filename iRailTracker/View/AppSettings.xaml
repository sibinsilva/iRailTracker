<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="iRailTracker.View.AppSettings"
             xmlns:converters="clr-namespace:iRailTracker.Converters"
             xmlns:vm="clr-namespace:iRailTracker.ViewModel"
             Title="Settings">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="24">

            <!-- AUTO REFRESH TOGGLE -->
            <Label Text="Auto Refresh"
                   FontAttributes="Bold"
                   FontSize="16" />

            <HorizontalStackLayout Spacing="20">

                <RadioButton Content="On"
             GroupName="AutoRefreshGroup"
             IsChecked="{Binding IsAutoRefreshEnabled}" />

                <RadioButton Content="Off"
             GroupName="AutoRefreshGroup"
             IsChecked="{Binding IsAutoRefreshEnabled, Converter={StaticResource InverseBoolConverter}}" />

            </HorizontalStackLayout>

            <!-- TIMER SECTION -->
            <Label Text="Auto Refresh Interval"
                   FontAttributes="Bold"
                   FontSize="16" />

            <Picker Title="Select interval"
                    ItemsSource="{Binding RefreshIntervals}"
                    ItemDisplayBinding="{Binding Display}"
                    SelectedItem="{Binding SelectedRefreshInterval}" 
                    IsEnabled="{Binding IsAutoRefreshEnabled}"/>

                    <!-- FAVOURITE STATIONS -->
            <Label Text="Favourite Stations"
                   FontAttributes="Bold"
                   FontSize="16" />

            <Label Text="Add favourite stations for quick access and delay alerts. The first one auto-loads on startup."
                   FontSize="13"
                   Opacity="0.7" />

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                <Picker Title="Select a station to add"
                        ItemsSource="{Binding StationNames}"
                        SelectedIndex="{Binding SelectedStationToAddIndex, Mode=TwoWay}"
                        FontSize="15"
                        Grid.Column="0" />

                <Button Text="Add"
                        Command="{Binding AddFavouriteCommand}"
                        FontSize="14"
                        Padding="12,8"
                        Grid.Column="1"
                        VerticalOptions="Center" />
            </Grid>

            <VerticalStackLayout Spacing="8" BindableLayout.ItemsSource="{Binding FavouriteStations}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Label Text="{Binding .}"
                                   FontSize="15"
                                   VerticalOptions="Center"
                                   Grid.Column="0" />

                            <Button Text="Remove"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:AppSettingsViewModel}}, Path=RemoveFavouriteCommand}"
                                    CommandParameter="{Binding .}"
                                    FontSize="13"
                                    Padding="10,6"
                                    Grid.Column="1" />
                        </Grid>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>

            <!-- DELAY NOTIFICATIONS -->
            <Label Text="Delay Notifications"
                   FontAttributes="Bold"
                   FontSize="16" />

            <Label Text="Get notified when a journey you're tracking (tap the bell on a journey card) is delayed past the threshold below."
                   FontSize="13"
                   Opacity="0.7" />

            <HorizontalStackLayout Spacing="20">

                <RadioButton Content="On"
             GroupName="DelayNotificationsGroup"
             IsChecked="{Binding IsDelayNotificationsEnabled}" />

                <RadioButton Content="Off"
             GroupName="DelayNotificationsGroup"
             IsChecked="{Binding IsDelayNotificationsEnabled, Converter={StaticResource InverseBoolConverter}}" />

            </HorizontalStackLayout>

            <Picker Title="Select delay threshold"
                    ItemsSource="{Binding DelayThresholds}"
                    ItemDisplayBinding="{Binding Display}"
                    SelectedItem="{Binding SelectedDelayThreshold}"
                    IsEnabled="{Binding IsDelayNotificationsEnabled}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>